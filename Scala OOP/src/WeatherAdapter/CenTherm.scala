package WeatherAdapter

import scala.util.Random

/**
 * @author Benton
 */
class CenTherm {
  // Return Centigrade
  def computeTemp(city: String) = {
    var random = new Random()
    random.nextInt(51)
  }
}